const tileSize = 32;
const mapWidthX = 40 * tileSize;
const mapHeightY = 30 * tileSize;

const assetsURL = "http://localhost:8080/api/resource/assets";
const singleAssetURL = "http://localhost:8080/api/resource/asset";
